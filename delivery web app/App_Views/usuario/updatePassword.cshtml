@using System.Data.SqlClient;

@{
    SqlConnection myConnection = new SqlConnection("Data Source=64.250.116.210,8333;Initial Catalog=Delivery;User ID=wilmer1104@yahoo.com;Password=Q1w2e3r4*/*");

    try
    {
        myConnection.Open();

        string Select = "UPDATE Mae_Autorizados SET clave='" + @Request["passwordUpdate"] + "'WHERE idrow =" + Session["id"] + "";
        SqlCommand cmd = new SqlCommand(Select, myConnection);
        SqlDataReader reader = cmd.ExecuteReader();

        <script type="text/javascript">
            function load() {
                window.location.assign("../../App_Views/index.cshtml");
            }
            window.onload = load;
        </script>

    }
    catch
    {
        <script type="text/javascript">
            alert("no se actualiso");        
            function load() {
                    window.location.assign("./usuario.cshtml");
            }
            window.onload = load;        
        </script>
    }


}