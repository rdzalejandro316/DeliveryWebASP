@{

    if (Session["User"].ToString().Trim().Equals(Session["Password"].ToString()))
    {
        //Response.Redirect("~/App_Views/usuario/usuario.cshtml");
        <script type="text/javascript">
            function load() {
            alert("contaseña y usuario son iguales");
        }
        //window.onload = load;
        </script>
    }
    else
    {
        <script type="text/javascript">
            function load() {
            alert("no son iguales el usuario y la contraseña");
        }
        //window.onload = load;
        </script>
    }
}